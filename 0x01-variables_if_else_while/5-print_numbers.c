#include <stdio.h>

/**
 * main - prints all single digit numbersof base 10 from 0 to 9
 * Reaturn: Always 0
 */
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
	printf("%d", num);
	printf("\n");

	return (0);
}
